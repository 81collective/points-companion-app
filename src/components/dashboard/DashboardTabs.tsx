"use client";
// Deprecated: DashboardTabs component removed in favor of simpler side navigation.
// Keeping file temporarily to avoid import errors during gradual refactor. Exports a noop.
export default function DashboardTabs() { return null; }
