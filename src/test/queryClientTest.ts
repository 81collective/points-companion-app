// Test file to verify React Query integration
import { QueryClient } from '@tanstack/react-query';

// Basic test to ensure React Query is properly configured
const queryClient = new QueryClient();
console.log('React Query configured successfully');

export default queryClient;
